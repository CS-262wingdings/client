<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="edu.calvin.cs262.wingdings.pigeonpoll">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".QuestionScreen" />
        <activity android:name=".QuestionAnswerScreen" />
        <activity android:name=".PassScreen1" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LastPlayerScreen" />
        <activity android:name=".ResultsScreen" />
        <activity
            android:name=".MainMenu"
            android:label="@string/title_activity_main_menu"
            android:theme="@style/AppTheme" />
        <activity android:name=".GameCustomization" />
        <activity android:name=".CustomQuestion" />
        <activity android:name=".How2Play" />
        <activity android:name=".YouWin" />
        <activity android:name=".DownloadQuestion"></activity>
    </application>

</manifest>